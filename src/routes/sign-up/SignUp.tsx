import { useEffect } from "react";

// Empty for now but could be our own webpage sometime
export default function SignUp() {
  useEffect(() => {
    location.replace(
      "https://netcompany.com/careers/denmark/events/pokerbot2025/"
    );
  }, []);

  return <></>;
}
